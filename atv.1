/*let matriz = [
    [1,2,3],
    [1,2,3],
    [1,2,3]
    ]
console.log(matriz[0][2])*/
const leitura = require(`readline-sync`)

matriz = []

for(let i=0;i<3;i++){
    matriz[i]=[]
    for(let j=0;j<3; j++){
        let valor = leitura.question("Digite um número para matriz:") 
        matriz[i][j] = valor
    console.log(`linha : ${i}, Coluna :${j} Matriz: ${matriz[i][j]}`)
